# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# env / secrets
.env
.env*.local
.env.local

# firebase CLI local cache (firebase.json / .firebaserc ARE committed)
.firebase/
firebase-debug.log*
database-debug.log*

# misc
.DS_Store
*.pem
npm-debug.log*

# secrets — Firebase Admin service account key (local dev)
service-account.json
*-service-account.json
serviceAccount*.json

# editor / local
CLAUDE.local.md
.claude/settings.local.json

# legacy static app (kept temporarily as a porting reference; removed at integration)
.vercel
.env*
